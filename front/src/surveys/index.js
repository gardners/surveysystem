import SleepCompanion from '../Sleepcompanion';


const Defaults = {
    validators {
        // TODO, #181
    },
};

const Surveys = {
    sleepcompanion: Object.assign(Defaults, SleepCompanion);
};


export default Surveys;
