sudo cp lighttpd.conf /etc/lighttpd/lighttpd.conf
sudo rm /var/log/lighttpd/error.log 
make && /usr/sbin/lighttpd -tt -f /etc/lighttpd/lighttpd.conf && service lighttpd stop && cp surveyfcgi /var/www/fastcgi && service lighttpd start
