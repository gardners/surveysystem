sudo cp lighttpd.conf /etc/lighttpd/lighttpd.conf
sudo rm /var/log/lighttpd/error.log 
make && ( cd ../front ; npm run build ) && /usr/sbin/lighttpd -tt -f /etc/lighttpd/lighttpd.conf && service lighttpd stop && cp surveyfcgi /var/www/fastcgi && service lighttpd start
